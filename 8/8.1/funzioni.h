#include <iostream>
#include <fstream>
#include <cstdlib>
#include <vector>
#include <cmath>

using namespace std;

double err (double, double, int);
double dist (const vector<double>&); // il metodo calcola r^2
double max (double, double);
double min (double, double);
void print (const vector<double>&);
